{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Oxidant","text":"<p>Agentic TypeScript \u2192 Rust translation harness</p> <p>Oxidant takes a TypeScript codebase and produces an idiomatic, compiling Rust codebase by orchestrating Claude Code subprocesses. It is not a transpiler \u2014 it controls agents. The agents do the actual conversion; Oxidant decides what to convert, in what order, with what context, and whether the output is acceptable.</p> <p>The primary test corpus is msagl-js \u2014 Microsoft's TypeScript graph layout engine (~4,800 functions). One of the project authors previously spent 236 commits hand-translating it; Oxidant automates that process.</p>"},{"location":"#why-this-approach-works","title":"Why this approach works","text":"<p>Naive file-by-file LLM prompting produces code that compiles in isolation but fails at integration. The approach is borrowed from the academic C-to-Rust literature (ORBIT, ENCRUST, SACTOR):</p> <ol> <li>Extract a full dependency graph first \u2014 every function knows what it calls and what calls it</li> <li>Translate in topological order \u2014 every dependency is already converted when a node is processed; the agent gets the real Rust signatures, not invented ones</li> <li>Verify each snippet with the real Rust compiler before accepting it</li> <li>Separate correctness from idiomaticity \u2014 Phase B produces working code; Phase C makes it idiomatic</li> </ol>"},{"location":"#the-four-phase-pipeline","title":"The four-phase pipeline","text":"<pre><code>flowchart LR\n    ts[(\"TypeScript&lt;br&gt;Repo\")]:::db\n\n    subgraph phaseA [\"PHASE A \u2014 Analysis\"]\n        direction TB\n        ast[\"ts-morph&lt;br&gt;AST extract\"] --&gt; idiom[\"Idiom&lt;br&gt;detection\"]\n        idiom --&gt; topo[\"Topological&lt;br&gt;sort\"]\n        topo --&gt; tier[\"Tier&lt;br&gt;classify\"]\n        tier --&gt; skel[\"Skeleton&lt;br&gt;generate\"]\n    end\n\n    subgraph phaseB [\"PHASE B \u2014 Translation Loop\"]\n        direction TB\n        pick[\"pick_next_node\"] --&gt; ctx[\"build_context\"]\n        ctx --&gt; agent[\"invoke_agent&lt;br&gt;(Claude Code)\"]:::ai\n        agent --&gt; ver[\"verify&lt;br&gt;stub \u00b7 branch \u00b7 cargo\"]\n        ver --&gt;|pass| upd[\"update_manifest\"]\n        ver --&gt;|fail| retry[\"retry / escalate\"]\n        retry --&gt; ctx\n        upd --&gt; pick\n    end\n\n    subgraph phaseC [\"PHASE C \u2014 Refinement\"]\n        direction TB\n        clippy[\"cargo clippy&lt;br&gt;--pedantic\"]\n        clippy --&gt; fixes[\"auto-fix&lt;br&gt;mechanical warnings\"]\n    end\n\n    subgraph phaseD [\"PHASE D \u2014 Integration\"]\n        direction TB\n        cbuild[\"cargo build&lt;br&gt;--release\"]\n        cbuild --&gt; equiv[\"equivalence&lt;br&gt;tests\"]\n    end\n\n    rs[(\"Rust&lt;br&gt;Crate\")]:::success\n\n    ts --&gt; phaseA\n    phaseA --&gt; phaseB\n    phaseB --&gt; phaseC\n    phaseC --&gt; phaseD\n    phaseD --&gt; rs\n\n    classDef db      fill:#7c2d12,stroke:#f97316,color:#fed7aa\n    classDef ai      fill:#4c1d95,stroke:#c084fc,color:#ddd6fe\n    classDef success fill:#065f46,stroke:#34d399,color:#a7f3d0</code></pre> <p>Phases are sequential at the top level. Phase B's internal loop is highly iterative \u2014 a node may be attempted multiple times at escalating model tiers before being accepted or queued for human review.</p> Phase What How A \u2014 Analysis AST extraction \u2192 idiom detection \u2192 topological sort \u2192 tier classification \u2192 Rust skeleton Deterministic (no AI) B \u2014 Translation Convert each node, verify, retry LangGraph loop + Claude Code subprocess C \u2014 Refinement Make the output idiomatic <code>cargo clippy</code> + targeted agents D \u2014 Integration Full build, equivalence tests <code>cargo build --release</code> + delta debugging"},{"location":"#current-status-msagl-js-corpus","title":"Current status (msagl-js corpus)","text":"Metric Value Total nodes 4,820 Auto-converted structural nodes 420 Haiku-tier nodes 1,196 Sonnet-tier nodes 3,476 Opus-tier nodes 148 Class hierarchies handled 22 (6 enums, 16 struct composition)"},{"location":"research/","title":"Research Background","text":"<p>Oxidant's approach is grounded in the academic literature on LLM-assisted code translation. The key references and lessons:</p>"},{"location":"research/#key-papers","title":"Key papers","text":"Paper Lesson applied ORBIT (2024) AST-driven, dependency-ordered approach substantially outperforms file-by-file LLM prompting His2Trans Skeleton-first approaches dramatically outperform node-by-node translation that builds from nothing SACTOR Splitting into a correctness-first pass followed by an idiomaticity pass produces better results than doing both simultaneously EvoC2Rust Topological ordering and real compiler feedback are the two biggest drivers of conversion quality ENCRUST Using <code>cargo check</code> for verification rather than an AST library catches borrow checker issues that static analysis misses <p>Full citations in RESEARCH_ACADEMIC.md.</p>"},{"location":"research/#prior-manual-translation","title":"Prior manual translation","text":"<p>Before Oxidant, the project authors attempted a manual translation of msagl-js to Rust over 236 commits. That experience provides direct ground truth about where agents cut corners and what the hard patterns are:</p> <ul> <li>Agents consistently simplify code rather than faithfully translating it (\"improvement during translation\" \u2014 documented in vjeux's Pokemon Showdown port study)</li> <li>The bidirectional Node\u2194Edge reference pattern is specifically hostile to Rust's ownership model and must be decided architecturally before any translation begins</li> <li>Deep class hierarchies require the scaffold (skeleton) to encode the correct Rust representation before agents touch the code \u2014 agents left to invent their own produce inconsistent results</li> </ul> <p>These lessons directly shaped Oxidant's design: the skeleton-first approach, the branch parity verification check, and the hierarchy classification system.</p>"},{"location":"pipeline/hierarchies/","title":"Class Hierarchy Handling","text":"<p>msagl-js has 101 classes that use <code>extends</code>, forming 22 distinct parent-child hierarchies. The skeleton generator classifies each hierarchy before emitting any Rust \u2014 because the correct Rust representation depends on why the hierarchy exists, not just that it exists.</p> <p>Classification is hardcoded in <code>KNOWN_HIERARCHIES</code> inside <code>analysis/hierarchy.py</code>, validated against a manual Rust port of msagl-js (<code>Routers/msagl-rust</code>). It is not a heuristic \u2014 every hierarchy was checked by hand.</p>"},{"location":"pipeline/hierarchies/#why-this-matters","title":"Why this matters","text":"<p>Agents have no memory between invocations. Without a hierarchy-aware skeleton, every agent in a family like <code>SweepEvent</code> independently invents its own Rust representation \u2014 producing inconsistent, incompatible code. The scaffold locks in the right structure before any agent touches the code.</p>"},{"location":"pipeline/hierarchies/#classification-decision-tree","title":"Classification decision tree","text":"<pre><code>flowchart TD\n    tsClass[\"TypeScript class&lt;br&gt;class Foo extends Bar\"]:::trigger\n\n    tsClass --&gt; inKnown{\"Bar in&lt;br&gt;KNOWN_HIERARCHIES?\"}:::decision\n\n    inKnown --&gt;|\"yes \u00b7 kind = enum\"| enumPath\n    inKnown --&gt;|\"yes \u00b7 kind = struct\"| structPath\n    inKnown --&gt;|no| checkParent{\"is Foo itself&lt;br&gt;a known base?\"}:::decision\n\n    checkParent --&gt;|\"yes \u00b7 kind = enum\"| enumBase\n    checkParent --&gt;|\"yes \u00b7 kind = struct\"| structBase\n    checkParent --&gt;|no| checkExternal{\"Bar in&lt;br&gt;manifest at all?\"}:::decision\n\n    checkExternal --&gt;|no| externalComment[\"pub struct Foo&lt;br&gt;// NOTE: extends Bar external\"]:::rustout\n    checkExternal --&gt;|yes| flatStruct[\"pub struct Foo&lt;br&gt;...own fields only\"]:::rustout\n\n    subgraph enumPath [\"Fold into parent enum\"]\n        direction TB\n        enumVariant[\"Foo becomes a variant&lt;br&gt;inside pub enum Bar\"]:::rustout\n        enumVariantFields{\"Foo has&lt;br&gt;fields?\"}:::decision\n        enumVariant --&gt; enumVariantFields\n        enumVariantFields --&gt;|yes| namedVariant[\"Bar::Foo { field_a: TypeA, field_b: TypeB }\"]:::rustout\n        enumVariantFields --&gt;|no| unitVariant[\"Bar::Foo (unit variant)\"]:::rustout\n        skipStruct[\"Foo gets NO pub struct&lt;br&gt;in foo.rs\"]:::stub\n        enumVariantFields --&gt; skipStruct\n    end\n\n    subgraph structPath [\"Struct composition\"]\n        direction TB\n        structChild[\"pub struct Foo {&lt;br&gt;    pub base: crate::bar_mod::Bar,&lt;br&gt;    ...own fields&lt;br&gt;}\"]:::rustout\n        superCall[\"super.method() calls&lt;br&gt;\u2192 self.base.method() in agent\"]:::neutral\n        structChild --&gt; superCall\n    end\n\n    subgraph enumBase [\"Emit enum in this module\"]\n        direction TB\n        collectChildren[\"collect all direct children&lt;br&gt;from HierarchyMap\"]:::process\n        emitEnum2[\"pub enum Foo {&lt;br&gt;    Child1 { fields },&lt;br&gt;    Child2(SubEnum),&lt;br&gt;    Child3,&lt;br&gt;}\"]:::rustout\n        collectChildren --&gt; emitEnum2\n    end\n\n    subgraph structBase [\"Is the parent \u2014 no change needed\"]\n        direction TB\n        parentNote[\"Foo is a hierarchy root.&lt;br&gt;Children will get pub base: Foo\"]:::neutral\n    end\n\n    classDef trigger  fill:#7c2d12,stroke:#f97316,color:#fed7aa\n    classDef decision fill:#422006,stroke:#f59e0b,color:#fef3c7\n    classDef rustout  fill:#78350f,stroke:#f59e0b,color:#fde68a\n    classDef stub     fill:#7f1d1d,stroke:#f87171,color:#fecaca\n    classDef neutral  fill:#1e293b,stroke:#64748b,color:#cbd5e1\n    classDef process  fill:#1e3a5f,stroke:#60a5fa,color:#bfdbfe</code></pre>"},{"location":"pipeline/hierarchies/#category-a-discriminated-unions-pub-enum","title":"Category A \u2014 Discriminated unions \u2192 <code>pub enum</code>","text":"<p>These hierarchies have subclasses that each add 0\u20133 unique fields. TypeScript code dispatches on them using <code>instanceof</code> checks. In Rust, these must be enums \u2014 a flat struct has no runtime discriminant to match on.</p> <p>Classified as enum: <code>SweepEvent</code>, <code>VertexEvent</code>, <code>BasicVertexEvent</code>, <code>BasicReflectionEvent</code>, <code>Layer</code>, <code>OptimalPacking</code></p> <p>The skeleton emits a single <code>pub enum</code> in the base class's <code>.rs</code> file. Each child class's fields become named fields of a variant. Child class <code>.rs</code> files emit no struct \u2014 they are folded into the parent enum.</p> TypeScript (11 separate classes)Rust skeleton (one enum in sweep_event.rs) <pre><code>class SweepEvent { }\n\nclass AxisCoordinateEvent extends SweepEvent {\n  site: Point;\n}\n\nclass ConeClosureEvent extends SweepEvent {\n  coneToCLose: Cone;\n  site: Point;\n}\n\nclass VertexEvent extends SweepEvent { }\n\nclass OpenVertexEvent extends VertexEvent {\n  vertex: PolylinePoint;\n}\n</code></pre> <pre><code>#[derive(Debug, Clone)]\npub enum SweepEvent {\n    AxisCoordinateEvent {\n        site: Rc&lt;RefCell&lt;crate::point::Point&gt;&gt;,\n    },\n    ConeClosureEvent {\n        cone_to_close: Rc&lt;RefCell&lt;crate::cone::Cone&gt;&gt;,\n        site: Rc&lt;RefCell&lt;crate::point::Point&gt;&gt;,\n    },\n    // Sub-hierarchy VertexEvent is itself an enum \u2014 referenced as a tuple variant\n    VertexEvent(crate::vertex_event::VertexEvent),\n    // ...\n}\n</code></pre>"},{"location":"pipeline/hierarchies/#sub-hierarchies","title":"Sub-hierarchies","text":"<p>When a child class is itself a hierarchy base (e.g. <code>VertexEvent</code> is a child of <code>SweepEvent</code> but also has its own subclasses), it gets its own <code>pub enum</code> in its own module and is referenced from the parent as a tuple variant:</p> <pre><code>// vertex_event.rs\n#[derive(Debug, Clone)]\npub enum VertexEvent {\n    BasicVertexEvent(crate::basic_vertex_event::BasicVertexEvent),\n    LeftVertexEvent,\n    LowestVertexEvent,\n    RightVertexEvent,\n}\n</code></pre>"},{"location":"pipeline/hierarchies/#type-redirect-table","title":"Type redirect table","text":"<p>When method signatures in other modules reference a now-folded child type (e.g. <code>PortObstacleEvent</code>), a redirect table (<code>_enum_child_redirect</code> in <code>generate_skeleton.py</code>) maps that name to the parent enum type (<code>crate::sweep_event::SweepEvent</code>) at type-mapping time, so cross-module method signatures still compile.</p>"},{"location":"pipeline/hierarchies/#category-b-behavior-hierarchies-struct-composition","title":"Category B \u2014 Behavior hierarchies \u2192 struct composition","text":"<p>These hierarchies have subclasses that are large independent classes sharing a base. Each subclass gets its own <code>pub struct</code> with <code>pub base: ParentType</code> as the first field.</p> <p>When agents convert methods that call <code>super.method()</code>, they write <code>self.base.method()</code>.</p> <p>Classified as struct:</p> Hierarchy Subclasses Notes <code>Algorithm</code> 24 Each is a large independent layout algorithm <code>Attribute</code> 4 avg 22.5 fields each <code>SegmentBase</code> 3 <code>LineSweeperBase</code> 3 <code>GeomObject</code> 3 <code>BasicGraphOnEdges</code> 2 <code>Entity</code> 3 Node/Edge/Graph used everywhere as distinct types <code>Port</code> 3 CurvePort/FloatingPort used as distinct port types <code>DrawingObject</code> 2 DrawingNode/DrawingEdge used standalone <code>SvgViewerObject</code> 2 Used standalone in viewer <code>ObstacleSide</code> 3 <code>BasicObstacleSide</code> 2 Referenced in SweepEvent fields <code>ConeSide</code> 2 ConeLeftSide/ConeRightSide used as field types <code>VisibilityEdge</code> 2 AxisEdge used as a field type <code>KdNode</code> 2 <code>Packing</code> 2 TypeScriptRust skeleton <pre><code>class SplineRouter extends Algorithm {\n  continueOnOverlaps: boolean;\n  obstacleCalculator: ShapeObstacleCalculator;\n\n  run() {\n    super.run();\n    // spline-specific logic\n  }\n}\n</code></pre> <pre><code>#[derive(Debug, Clone)]\npub struct SplineRouter {\n    // base field always first; cross-module path\n    pub base: crate::algorithm::Algorithm,\n    pub continue_on_overlaps: bool,\n    pub obstacle_calculator: Rc&lt;RefCell&lt;\n        crate::shape_obstacle_calculator::ShapeObstacleCalculator\n    &gt;&gt;,\n}\n\n// Agent writes: self.base.run();\n</code></pre>"},{"location":"pipeline/hierarchies/#external-parents","title":"External parents","text":"<p>If a class extends a type that is not in the manifest corpus (e.g. browser built-ins like <code>EventSource</code>), the skeleton emits a comment rather than a field:</p> <pre><code>pub struct MyClass {\n    // NOTE: extends EventSource (external \u2014 not in corpus)\n    pub some_field: String,\n}\n</code></pre>"},{"location":"pipeline/phase-a/","title":"Phase A \u2014 Analysis &amp; Skeleton Generation","text":"<p>Phase A is entirely deterministic \u2014 no AI involvement. It produces all the structured inputs Phase B requires.</p>"},{"location":"pipeline/phase-a/#a1-ast-extraction-phase_a_scriptsextract_astts","title":"A1 \u2014 AST Extraction (<code>phase_a_scripts/extract_ast.ts</code>)","text":"<p>A ts-morph TypeScript script parses the entire source codebase and populates <code>oxidant.db</code>. ts-morph was chosen specifically for its cross-file type resolution \u2014 when a method in <code>GeomGraph</code> accepts a <code>GeomNode</code> parameter from another module, ts-morph resolves that type fully and records it as a dependency edge.</p> <p>Node kinds extracted:</p> Kind Description <code>class</code> Class definition (fields only, no method bodies) <code>constructor</code> Constructor body <code>method</code>, <code>getter</code>, <code>setter</code> Individual method bodies <code>free_function</code> Module-level functions <code>interface</code> TypeScript interface (becomes a Rust trait) <code>enum</code> TypeScript enum (becomes a Rust enum) <code>type_alias</code> TypeScript type aliases <p>For each node, the extractor records: unique node ID, source file path, line range, full source text, parameter types, return type, type-level and call-level dependency edges, cyclomatic complexity, and BFS level.</p>"},{"location":"pipeline/phase-a/#a2-idiom-detection-phase_a_scriptsdetect_idiomsts","title":"A2 \u2014 Idiom Detection (<code>phase_a_scripts/detect_idioms.ts</code>)","text":"<p>A second ts-morph pass scans each node's AST for 14 patterns known to require non-trivial Rust translation. Each detected pattern is stored in <code>idioms_needed[]</code> on the node and used in Phase B to inject relevant guidance into the conversion prompt.</p> <p>See Idiom Detection for the full list and translation guidance.</p>"},{"location":"pipeline/phase-a/#a3-topological-sort","title":"A3 \u2014 Topological Sort","text":"<p>Nodes are sorted so that when a node is translated, every node it depends on has already been translated. The resulting Rust signatures of dependencies are available to the agent in the prompt \u2014 eliminating guesswork about what the API looks like. This is the single biggest driver of translation quality.</p>"},{"location":"pipeline/phase-a/#a4-tier-classification-analysisclassify_tierspy","title":"A4 \u2014 Tier Classification (<code>analysis/classify_tiers.py</code>)","text":"<p>Each node is assigned a translation tier based on cyclomatic complexity, idiom count, and node kind:</p> Tier Model Nodes (msagl-js) Max attempts <code>haiku</code> claude-haiku-4-5 1,196 3 <code>sonnet</code> claude-sonnet-4-6 3,476 4 <code>opus</code> claude-opus-4-6 148 5 <p>If a node exhausts its retry budget it escalates to the next tier. A haiku node that fails 3 times is re-tried as sonnet; sonnet after 4 failures escalates to opus; opus after 5 failures goes to the human review queue.</p>"},{"location":"pipeline/phase-a/#a5-skeleton-generation-analysisgenerate_skeletonpy","title":"A5 \u2014 Skeleton Generation (<code>analysis/generate_skeleton.py</code>)","text":"<pre><code>flowchart TD\n    manifest[(\"oxidant.db\")]:::db\n    manifest --&gt; loadhier[\"Build HierarchyMap&lt;br&gt;scan all CLASS nodes&lt;br&gt;detect extends relationships\"]:::process\n\n    loadhier --&gt; knownH{{\"KNOWN_HIERARCHIES&lt;br&gt;22 classified bases\"}}:::decision\n\n    knownH --&gt; enumBases[\"Enum bases:&lt;br&gt;SweepEvent \u00b7 VertexEvent&lt;br&gt;BasicVertexEvent \u00b7 BasicReflectionEvent&lt;br&gt;Layer \u00b7 OptimalPacking\"]:::trigger\n    knownH --&gt; structBases[\"Struct bases:&lt;br&gt;Algorithm \u00b7 Attribute \u00b7 Entity&lt;br&gt;Port \u00b7 GeomObject \u00b7 ConeSide&lt;br&gt;and 10 more\"]:::neutral\n\n    subgraph perModule [\"For each .ts module \u2192 .rs file\"]\n        direction TD\n\n        ifaces[\"Emit pub trait&lt;br&gt;for each interface\"]:::process\n\n        subgraph enumPass [\"Enum pre-pass\"]\n            direction LR\n            findEnumBase{\"is class an&lt;br&gt;enum base?\"}:::decision\n            collectVariants[\"collect child classes&lt;br&gt;extract each child's fields\"]:::process\n            emitEnum[\"emit pub enum Base&lt;br&gt;Variant1 { field }&lt;br&gt;SubEnumChild(sub_enum)&lt;br&gt;ZeroFieldVariant\"]:::rustout\n            findEnumBase --&gt;|yes| collectVariants --&gt; emitEnum\n            findEnumBase --&gt;|no| skipEnum[\"skip\"]\n        end\n\n        subgraph classLoop [\"Class struct loop\"]\n            direction TD\n            isEnumBase2{\"is enum base?\"}:::decision\n            isEnumBase2 --&gt;|yes| skipBase[\"skip&lt;br&gt;already emitted above\"]\n            isEnumBase2 --&gt;|no| isEnumChild{\"is child of&lt;br&gt;enum hierarchy?\"}:::decision\n            isEnumChild --&gt;|yes| skipChild[\"skip&lt;br&gt;folded into parent enum\"]\n            isEnumChild --&gt;|no| isStructChild{\"is child of&lt;br&gt;struct hierarchy?\"}:::decision\n            isStructChild --&gt;|yes| emitStructChild[\"emit pub struct Child&lt;br&gt;pub base: crate::mod::Parent&lt;br&gt;...own fields\"]:::rustout\n            isStructChild --&gt;|no| hasExtParent{\"extends external&lt;br&gt;type?\"}:::decision\n            hasExtParent --&gt;|yes| emitComment[\"emit pub struct&lt;br&gt;// NOTE: extends Foo external\"]:::rustout\n            hasExtParent --&gt;|no| emitFlat[\"emit pub struct Flat&lt;br&gt;...own fields\"]:::rustout\n        end\n\n        subgraph fnPass [\"Function stubs\"]\n            emitFn[\"emit pub fn signature&lt;br&gt;todo! OXIDANT: not yet translated\"]:::stub\n        end\n\n        ifaces --&gt; enumPass\n        enumPass --&gt; classLoop\n        classLoop --&gt; fnPass\n    end\n\n    subgraph redirect [\"Type redirect table\"]\n        enumChildRedirect[\"_enum_child_redirect&lt;br&gt;PortObstacleEvent \u2192 crate::sweep_event::SweepEvent&lt;br&gt;OpenVertexEvent \u2192 crate::vertex_event::VertexEvent&lt;br&gt;...all folded child names\"]:::neutral\n    end\n\n    loadhier --&gt; perModule\n    loadhier --&gt; redirect\n    redirect -.-&gt;|\"applied during&lt;br&gt;type mapping\"| perModule\n\n    skeleton[(\"corpora/msagl-rs/&lt;br&gt;cargo check passes\")]:::success\n\n    perModule --&gt; skeleton\n\n    classDef db       fill:#7c2d12,stroke:#f97316,color:#fed7aa\n    classDef process  fill:#1e3a5f,stroke:#60a5fa,color:#bfdbfe\n    classDef decision fill:#422006,stroke:#f59e0b,color:#fef3c7\n    classDef trigger  fill:#7c2d12,stroke:#f97316,color:#fed7aa\n    classDef neutral  fill:#1e293b,stroke:#64748b,color:#cbd5e1\n    classDef rustout  fill:#78350f,stroke:#f59e0b,color:#fde68a\n    classDef stub     fill:#7f1d1d,stroke:#f87171,color:#fecaca\n    classDef success  fill:#065f46,stroke:#34d399,color:#a7f3d0</code></pre> <p>A Python script reads <code>oxidant.db</code> and writes a complete, compilable Rust project to <code>corpora/msagl-rs/</code>. The skeleton must pass <code>cargo check</code> before Phase B begins.</p> <p>What the skeleton contains:</p> <ul> <li><code>Cargo.toml</code> with the approved crate inventory</li> <li><code>src/lib.rs</code> with <code>pub mod</code> declarations for every module</li> <li>One <code>.rs</code> file per TypeScript source file</li> <li><code>pub struct</code> / <code>pub enum</code> / <code>pub trait</code> / <code>pub fn</code> declarations for every node</li> <li><code>todo!(\"OXIDANT: not yet translated \u2014 &lt;node_id&gt;\")</code> as the body of every function</li> </ul> <p>The count of remaining <code>todo!()</code> macros is the primary progress metric throughout Phase B.</p>"},{"location":"pipeline/phase-a/#typescript-rust-type-mapping","title":"TypeScript \u2192 Rust type mapping","text":"<p>Cross-module references use fully-qualified <code>crate::module::Type</code> paths so no <code>use</code> imports are needed.</p> TypeScript Rust <code>number</code> <code>f64</code> <code>string</code> <code>String</code> <code>boolean</code> <code>bool</code> <code>T[]</code> / <code>Array&lt;T&gt;</code> <code>Vec&lt;T&gt;</code> <code>Map&lt;K,V&gt;</code> <code>std::collections::HashMap&lt;K,V&gt;</code> <code>Set&lt;T&gt;</code> <code>std::collections::HashSet&lt;T&gt;</code> <code>T \\| null</code> <code>Option&lt;T&gt;</code> DOM/Web API types <code>serde_json::Value</code> User class <code>Foo</code> (same module) <code>Rc&lt;RefCell&lt;Foo&gt;&gt;</code> User class <code>Foo</code> (other module) <code>Rc&lt;RefCell&lt;crate::module::Foo&gt;&gt;</code> TypeScript interface <code>IFoo</code> <code>Rc&lt;dyn IFoo&gt;</code> <p>For class hierarchies, the skeleton emits <code>pub enum</code> or <code>pub base:</code> fields depending on the hierarchy kind. See Class Hierarchies.</p>"},{"location":"pipeline/phase-b/","title":"Phase B \u2014 Translation Loop","text":"<p>Phase B is a LangGraph <code>StateGraph</code> that iterates over the manifest in topological order. Each iteration picks one eligible node, builds a context-rich prompt, calls Claude Code as a subprocess, verifies the output, and either accepts it or retries.</p>"},{"location":"pipeline/phase-b/#graph-structure","title":"Graph structure","text":"<pre><code>flowchart TD\n    start([\"START\"]):::trigger\n\n    start --&gt; pick[\"pick_next_node&lt;br&gt;atomic SELECT + UPDATE&lt;br&gt;in SQLite WAL transaction\"]:::process\n\n    pick --&gt; eligible{\"eligible node&lt;br&gt;found?\"}:::decision\n    eligible --&gt;|no| done([\"DONE \u2014 Phase B complete\"]):::success\n\n    eligible --&gt;|yes| ctx[\"build_context&lt;br&gt;TypeScript source path&lt;br&gt;Rust skeleton path&lt;br&gt;converted dependency snippets&lt;br&gt;idiom dictionary entries&lt;br&gt;retry error if attempt &gt; 0\"]:::process\n\n    ctx --&gt; agent[\"invoke_agent&lt;br&gt;claude --print&lt;br&gt;ANTHROPIC_API_KEY stripped\"]:::ai\n\n    agent --&gt; verify[\"verify&lt;br&gt;3-step pipeline\"]:::process\n\n    verify --&gt; stub{\"contains&lt;br&gt;todo! or&lt;br&gt;unimplemented!?\"}:::decision\n    stub --&gt;|yes| stubFail&gt;\"STUB fail\"]:::error\n\n    stub --&gt;|no| branch{\"Rust branches \u2265 60%&lt;br&gt;of TS branches?\"}:::decision\n    branch --&gt;|no| branchFail&gt;\"BRANCH fail\"]:::error\n\n    branch --&gt;|yes| cargo[\"inject into skeleton&lt;br&gt;cargo check&lt;br&gt;restore original\"]:::process\n    cargo --&gt; cargoPass{\"cargo check&lt;br&gt;passes?\"}:::decision\n\n    cargoPass --&gt;|\"cascade failure&lt;br&gt;other file broken\"| cascade&gt;\"CASCADE&lt;br&gt;defer node\"]:::error\n    cargoPass --&gt;|yes| pass[\"update_manifest&lt;br&gt;status = converted&lt;br&gt;save snippet_path\"]:::success\n    pass --&gt; pick\n\n    cargoPass --&gt;|no| cargoFail&gt;\"CARGO fail&lt;br&gt;compiler error\"]:::error\n\n    stubFail --&gt; attempts{\"attempts &lt; max&lt;br&gt;for tier?\"}:::decision\n    branchFail --&gt; attempts\n    cargoFail --&gt; attempts\n    cascade --&gt; pick\n\n    attempts --&gt;|yes| retry[\"retry_node&lt;br&gt;append error to context&lt;br&gt;increment attempt_count\"]:::process\n    retry --&gt; ctx\n\n    attempts --&gt;|\"haiku exhausted\"| escalate[\"escalate_node&lt;br&gt;haiku \u2192 sonnet&lt;br&gt;sonnet \u2192 opus\"]:::process\n    escalate --&gt; ctx\n\n    attempts --&gt;|\"all tiers exhausted\"| supervisor[\"supervisor_node&lt;br&gt;LLM reviews all failures&lt;br&gt;generates hint or skips\"]:::ai\n    supervisor --&gt; supRoute{\"supervisor&lt;br&gt;decision\"}:::decision\n    supRoute --&gt;|retry with hint| ctx\n    supRoute --&gt;|skip| queue[\"queue_for_review&lt;br&gt;write to review_queue\"]:::error\n    queue --&gt; pick\n\n    classDef trigger  fill:#7c2d12,stroke:#f97316,color:#fed7aa\n    classDef process  fill:#1e3a5f,stroke:#60a5fa,color:#bfdbfe\n    classDef ai       fill:#4c1d95,stroke:#c084fc,color:#ddd6fe\n    classDef decision fill:#422006,stroke:#f59e0b,color:#fef3c7\n    classDef success  fill:#065f46,stroke:#34d399,color:#a7f3d0\n    classDef error    fill:#7f1d1d,stroke:#f87171,color:#fecaca</code></pre>"},{"location":"pipeline/phase-b/#node-descriptions","title":"Node descriptions","text":"<pre><code>pick_next_node\n  \u2502  Atomically claims the next eligible node from SQLite.\n  \u2502  Eligible = NOT_STARTED + all dependencies are CONVERTED.\n  \u2502  In single-worker mode, also resets orphaned IN_PROGRESS nodes.\n  \u2193 (done \u2192 END)\n\nbuild_context\n  \u2502  Assembles the full conversion prompt from the manifest node,\n  \u2502  dependency snippets, idiom entries, and any retry context.\n  \u2193\n\ninvoke_agent\n  \u2502  Runs: claude --print --output-format json &lt;prompt&gt;\n  \u2502  ANTHROPIC_API_KEY is stripped before invocation (see below).\n  \u2193\n\nverify\n  \u2502  Three checks: stub \u2192 branch parity \u2192 cargo check.\n  \u2193 (conditional routing)\n\n  \u251c\u2500 PASS \u2192 update_manifest \u2192 pick_next_node\n  \u2502\n  \u251c\u2500 STUB / BRANCH (under attempt limit)\n  \u2502     \u2192 retry_node \u2192 build_context (with error context)\n  \u2502\n  \u251c\u2500 CARGO (under attempt limit)\n  \u2502     \u2192 retry_node \u2192 build_context (with compiler error)\n  \u2502\n  \u251c\u2500 (haiku limit hit) \u2192 escalate_node (\u2192 sonnet) \u2192 build_context\n  \u251c\u2500 (sonnet limit hit) \u2192 escalate_node (\u2192 opus) \u2192 build_context\n  \u251c\u2500 (opus limit hit) \u2192 supervisor_node \u2192 build_context or queue_for_review\n  \u2502\n  \u2514\u2500 (all tiers exhausted) \u2192 queue_for_review \u2192 pick_next_node\n</code></pre>"},{"location":"pipeline/phase-b/#prompt-structure","title":"Prompt structure","text":"<p>Every prompt is assembled fresh for each attempt by <code>agents/context.py</code>. The prompt tells the agent to:</p> <ol> <li>Read the TypeScript source file to understand the function</li> <li>Read the Rust skeleton file to see available types and signatures</li> <li>Implement only the one function body marked by <code>todo!(\"OXIDANT: not yet translated \u2014 &lt;node_id&gt;\")</code></li> <li>Run <code>cargo check</code> in the skeleton directory and fix errors</li> <li>Output the final function body text (no markdown fences) when done</li> </ol> <p>Prompt sections:</p> Section Always present Contents Task description Yes Which node, which files, the todo marker to replace Rules Yes No <code>todo!()</code>, pure ASCII, faithful translation, only approved crates Architectural Decisions Yes From <code>oxidant.config.json</code> (e.g. graph ownership strategy) Converted Dependencies If deps exist Rust snippet bodies for all functions this node calls Idiom Translations If idioms detected Relevant sections from <code>idiom_dictionary.md</code> Previous Attempt Failed On retry The exact cargo check or verification error Supervisor Hint If provided Hint injected after supervisor review"},{"location":"pipeline/phase-b/#converted-dependencies","title":"Converted dependencies","text":"<p>The <code>## Converted Dependencies</code> section is loaded by <code>_load_dep_snippets()</code> in <code>context.py</code>. It follows both <code>call_dependencies</code> and <code>type_dependencies</code> from the manifest node, looks up each dependency's <code>snippet_path</code> in the DB, and reads the saved <code>.rs</code> file from disk.</p> <p>This means agents always see the actual Rust implementation of everything they call \u2014 not just a signature stub. Topological ordering guarantees every dependency is already converted before the current node is attempted.</p>"},{"location":"pipeline/phase-b/#max-subscription-auth","title":"Max subscription auth","text":"<p>Claude Code is invoked as a subprocess, not via the Python SDK:</p> <pre><code>env = os.environ.copy()\nenv.pop(\"ANTHROPIC_API_KEY\", None)   # force Max subscription auth\n\nsubprocess.run(\n    [\"claude\", \"--print\", \"--output-format\", \"json\", prompt],\n    env=env,\n    cwd=workspace,\n    timeout=300,\n)\n</code></pre> <p>Stripping <code>ANTHROPIC_API_KEY</code> forces Claude Code to use the user's Max subscription rather than billing to the API account. If the key is present, charges accrue to the API account \u2014 this has caused accidental charges of $1,800+ for other users.</p>"},{"location":"pipeline/phase-b/#parallelism","title":"Parallelism","text":"<p>Multiple worker processes can run simultaneously. Each worker uses its own clone of the skeleton project at <code>target_path/.clone_N/</code> so concurrent <code>cargo check</code> invocations don't race on the same files. The <code>claim_next_eligible()</code> SQL transaction is atomic \u2014 no two workers ever claim the same node.</p> <p><code>setup_worker_clones()</code> in <code>graph/nodes.py</code> creates the clones at startup by copying <code>src/</code> and <code>Cargo.toml</code>.</p>"},{"location":"pipeline/phase-b/#serving-mode-oxidant-serve","title":"Serving mode (<code>oxidant serve</code>)","text":"<p><code>oxidant serve</code> starts a FastAPI server that exposes Phase B over HTTP, allowing a Vue 3 GUI to control and monitor runs without a terminal. In serve mode, the graph uses <code>SqliteSaver</code> as the LangGraph checkpointer, enabling durable resume of interrupted runs.</p> <p>Endpoints:</p> Endpoint Description <code>POST /run</code> Start or resume a Phase B run <code>GET /stream/{thread_id}</code> SSE stream of progress events <code>POST /pause/{thread_id}</code> Pause after the current node <code>POST /resume/{thread_id}</code> Resume after a supervisor interrupt <code>GET /review-queue</code> Nodes awaiting human review <code>GET /status/{thread_id}</code> Run status snapshot"},{"location":"pipeline/phase-c/","title":"Phase C \u2014 Idiomatic Refinement","text":"<p>Phase C runs after Phase B completes (all nodes converted or in the human review queue). Its goal is idiomaticity \u2014 making the output good Rust, not just correct Rust. It does not change behavior; every change must pass the full test suite.</p>"},{"location":"pipeline/phase-c/#approach","title":"Approach","text":"<p>Phase B agents are instructed to produce faithful, correct translations. They use <code>Rc&lt;RefCell&lt;T&gt;&gt;</code> everywhere, <code>.clone()</code> freely, and don't restructure logic. This is intentional \u2014 correctness first, style second. Phase C then makes a targeted refinement pass.</p> <p>Phase C runs:</p> <pre><code>cargo clippy --all-targets -- -W clippy::pedantic\n</code></pre> <p>Warnings are categorized by fix type:</p> Category Examples Handler Mechanical Redundant clones, unnecessary <code>mut</code>, simple iterator improvements Haiku agent auto-fixes Structural Replace <code>Rc&lt;RefCell&lt;&gt;&gt;</code> with ownership, arena allocation improvements Sonnet agent proposes; human approves Human judgment Anything requiring architectural decisions Surfaced to review queue"},{"location":"pipeline/phase-c/#files","title":"Files","text":"File Role <code>refinement/clippy_runner.py</code> Runs <code>cargo clippy</code>, captures output <code>refinement/categorize.py</code> Classifies each warning by fix category <code>refinement/phase_c.py</code> Orchestrates the refinement loop"},{"location":"pipeline/verification/","title":"Verification","text":"<pre><code>flowchart TD\n    snippet[/\"Rust snippet&lt;br&gt;returned by agent\"/]:::trigger\n\n    snippet --&gt; s1[\"Step 1 \u2014 Stub check&lt;br&gt;grep for todo! unimplemented!&lt;br&gt;instant \u00b7 no disk I/O\"]:::process\n\n    s1 --&gt; stubFound{\"stub&lt;br&gt;found?\"}:::decision\n    stubFound --&gt;|yes| stubFail([\"STUB \u2014 retry\"]):::error\n\n    stubFound --&gt;|no| s2[\"Step 2 \u2014 Branch parity&lt;br&gt;count if else switch for while ? in TS&lt;br&gt;count if else match for while loop in Rust\"]:::process\n\n    s2 --&gt; branchCheck{\"TS branches \u2265 3&lt;br&gt;AND&lt;br&gt;Rust count &lt; 60%&lt;br&gt;of TS count?\"}:::decision\n    branchCheck --&gt;|yes| branchFail([\"BRANCH \u2014 retry&lt;br&gt;with count diagnostic\"]):::error\n\n    branchCheck --&gt;|no| s3[\"Step 3 \u2014 cargo check&lt;br&gt;locate todo! marker for node_id&lt;br&gt;replace with snippet&lt;br&gt;run cargo check --message-format short&lt;br&gt;restore original in finally block\"]:::process\n\n    s3 --&gt; cargoRun{\"cargo check&lt;br&gt;return code\"}:::decision\n\n    cargoRun --&gt;|\"0 \u2014 pass\"| pass([\"PASS \u2014 accept snippet\"]):::success\n\n    cargoRun --&gt;|\"non-zero\"| cascade{\"every error line&lt;br&gt;in a DIFFERENT file?\"}:::decision\n\n    cascade --&gt;|yes| cascadeOut([\"CASCADE \u2014 defer&lt;br&gt;prior snippet is broken\"]):::error\n    cascade --&gt;|no| cargoFail([\"CARGO \u2014 retry&lt;br&gt;with compiler error\"]):::error\n\n    classDef trigger  fill:#7c2d12,stroke:#f97316,color:#fed7aa\n    classDef process  fill:#1e3a5f,stroke:#60a5fa,color:#bfdbfe\n    classDef decision fill:#422006,stroke:#f59e0b,color:#fef3c7\n    classDef success  fill:#065f46,stroke:#34d399,color:#a7f3d0\n    classDef error    fill:#7f1d1d,stroke:#f87171,color:#fecaca</code></pre> <p>Every translated snippet passes three checks before being accepted, ordered cheapest-first. The first failure short-circuits \u2014 no point running <code>cargo check</code> if the snippet still contains <code>todo!()</code>.</p>"},{"location":"pipeline/verification/#step-1-stub-check","title":"Step 1 \u2014 Stub check","text":"<p>Greps for <code>todo!()</code> or <code>unimplemented!()</code> in the returned snippet. Agents are explicitly instructed not to use these, but they occasionally slip through on complex nodes. Instant fail.</p>"},{"location":"pipeline/verification/#step-2-branch-parity","title":"Step 2 \u2014 Branch parity","text":"<p>Counts branch constructs in both the TypeScript source and the Rust output and checks that the Rust has at least 60% as many:</p> Language Constructs counted TypeScript <code>if</code>, <code>else</code>, <code>switch</code>, <code>case</code>, <code>for</code>, <code>while</code>, <code>?</code> (ternary) Rust <code>if</code>, <code>else</code>, <code>match</code>, <code>for</code>, <code>while</code>, <code>loop</code> <p>The check only triggers when the TypeScript source has 3 or more branch constructs. Below that threshold, a simpler Rust translation is plausible (e.g. a single <code>if/else</code> chain becoming a <code>match</code>).</p> <p>Why this exists: LLM agents consistently try to simplify code during translation \u2014 collapsing branches, dropping edge cases, restructuring logic. This check catches the most common form of that failure (missing branches) cheaply, before spending time on cargo check.</p>"},{"location":"pipeline/verification/#step-3-cargo-check-inject-and-restore","title":"Step 3 \u2014 cargo check (inject and restore)","text":"<pre><code>1. Locate todo!(\"OXIDANT: not yet translated \u2014 &lt;node_id&gt;\") in the skeleton .rs file\n2. Replace it with the snippet\n3. Run: cargo check --message-format=short  (whole project)\n4. Always restore the original file (finally block \u2014 even on crash)\n5. Classify the result\n</code></pre> <p>Why <code>cargo check</code> on the whole project, not just the file?</p> <p>Every unconverted function in the skeleton is a <code>todo!()</code> stub with a correct signature. When the new snippet is injected and <code>cargo check</code> runs, it type-checks the snippet against the real API of everything it touches \u2014 other structs, traits, enums \u2014 catching type boundary mismatches immediately rather than at final integration.</p> <p>Cascade detection: If <code>cargo check</code> fails but every error line implicates a file other than the target, the failure is a cascade from a previously-converted (broken) snippet, not from the new snippet. The result is classified as <code>CASCADE</code> rather than <code>CARGO</code> \u2014 the node is deferred and retried later when the broken dependency is fixed.</p>"},{"location":"pipeline/verification/#smoke-check-on-restore-failure","title":"Smoke check on restore failure","text":"<p>If the original file cannot be restored cleanly (which would leave the skeleton in a broken state), an error is logged at <code>ERROR</code> level. This is a safeguard \u2014 it should never happen under normal operation, but if it does, manual intervention is required before continuing.</p>"},{"location":"pipeline/verification/#result-routing","title":"Result routing","text":"Status Meaning Action <code>PASS</code> All three checks passed Accept snippet, mark node <code>converted</code> <code>STUB</code> <code>todo!()</code> found in snippet Retry with error context <code>BRANCH</code> Too few branches vs TypeScript Retry with branch count diagnostics <code>CARGO</code> Compilation error in target file Retry with exact compiler error <code>CASCADE</code> Compilation error in other file Defer; retry later"},{"location":"reference/cli/","title":"CLI Reference","text":"<p>Oxidant is invoked via the <code>oxidant</code> command (installed via <code>uv run oxidant</code> or <code>pip install -e .</code>).</p>"},{"location":"reference/cli/#phase-a","title":"Phase A","text":"<pre><code># Full Phase A: extract AST, detect idioms, topological sort,\n# Haiku tier classification, generate Rust skeleton\noxidant phase-a\n\n# Skip AI tier classification \u2014 use heuristic rules instead\noxidant phase-a --heuristic-tiers\n\n# Regenerate the Rust skeleton only (manifest already exists)\noxidant generate-skeleton\n\n# Reclassify tiers on an existing manifest (no re-extraction)\noxidant classify-tiers\noxidant classify-tiers --heuristic\n</code></pre>"},{"location":"reference/cli/#phase-b","title":"Phase B","text":"<pre><code># Translate all nodes\noxidant phase-b\n\n# Stop after N nodes (smoke test)\noxidant phase-b --max-nodes 5\n\n# Print the first node's prompt without making any API calls\noxidant phase-b --dry-run\n\n# Run with parallel workers\noxidant phase-b --parallelism 4\n</code></pre>"},{"location":"reference/cli/#serve-mode","title":"Serve mode","text":"<pre><code># Start the FastAPI server (GUI + HTTP API for Phase B)\noxidant serve\noxidant serve --port 8080\n</code></pre>"},{"location":"reference/cli/#phase-c-d","title":"Phase C / D","text":"<pre><code># Phase C \u2014 Clippy-driven idiomatic refinement\noxidant phase-c\n\n# Phase D \u2014 Full build + integration error isolation\noxidant phase-d\n</code></pre>"},{"location":"reference/data-store/","title":"Data Store","text":"<p>All conversion state lives in a single SQLite file: <code>oxidant.db</code>. It is the source of truth for all phases.</p>"},{"location":"reference/data-store/#tables","title":"Tables","text":""},{"location":"reference/data-store/#nodes","title":"<code>nodes</code>","text":"<p>One row per manifest node.</p> Column Type Description <code>node_id</code> TEXT PK e.g. <code>SweepEvent__addEvent</code> <code>source_file</code> TEXT Relative TypeScript path, e.g. <code>modules/layout/src/sweep_event.ts</code> <code>line_start</code> / <code>line_end</code> INT Line range in the source file <code>source_text</code> TEXT Full TypeScript source of this node <code>node_kind</code> TEXT <code>class \\| method \\| constructor \\| getter \\| setter \\| free_function \\| interface \\| enum \\| type_alias</code> <code>parameter_types</code> JSON <code>{\"paramName\": \"TypeScript type string\"}</code> <code>return_type</code> TEXT TypeScript return type string <code>type_dependencies</code> JSON Node IDs this node references by type <code>call_dependencies</code> JSON Node IDs this node calls at runtime <code>callers</code> JSON Node IDs that call this node <code>parent_class</code> TEXT Class name if this is a method/constructor <code>cyclomatic_complexity</code> INT Number of independent paths through the code <code>idioms_needed</code> JSON Idiom tags detected by <code>detect_idioms.ts</code> <code>topological_order</code> INT Position in dependency-sorted order <code>bfs_level</code> INT BFS level (for parallelism grouping) <code>tier</code> TEXT <code>haiku \\| sonnet \\| opus</code> <code>status</code> TEXT <code>not_started \\| in_progress \\| converted \\| failed \\| human_review</code> <code>snippet_path</code> TEXT Path to the saved <code>.rs</code> snippet once converted <code>attempt_count</code> INT Number of conversion attempts made <code>last_error</code> TEXT Error from most recent failed attempt"},{"location":"reference/data-store/#manifest_meta","title":"<code>manifest_meta</code>","text":"<p>Single-row metadata: source repo path, schema version, generation timestamp.</p>"},{"location":"reference/data-store/#concurrency","title":"Concurrency","text":"<p>The DB uses WAL mode (<code>PRAGMA journal_mode=WAL</code>), which allows concurrent readers alongside a single writer. This is what makes parallel workers safe.</p> <p>The <code>claim_next_eligible()</code> method performs an atomic <code>SELECT + UPDATE</code> in a single transaction, so no two workers ever claim the same node.</p>"},{"location":"reference/data-store/#accessing-the-manifest","title":"Accessing the manifest","text":"<p>All code goes through the <code>Manifest</code> class in <code>models/manifest.py</code>:</p> <pre><code>from oxidant.models.manifest import Manifest\nfrom pathlib import Path\n\nm = Manifest.load(Path(\"oxidant.db\"))\n\n# Read a node\nnode = m.nodes[\"SweepEvent__addEvent\"]\n\n# Update node status (single-row write, not a full file rewrite)\nm.update_node(Path(\"oxidant.db\"), node.node_id, status=NodeStatus.CONVERTED)\n\n# Claim the next eligible node atomically\nnode = m.claim_next_eligible()\n</code></pre> <p>The <code>nodes</code> property performs a <code>SELECT *</code> \u2014 use sparingly in hot loops. <code>claim_next_eligible()</code> uses a targeted SQL query.</p>"},{"location":"reference/idioms/","title":"Idiom Detection","text":"<p>TypeScript has patterns with no direct Rust equivalent. Oxidant detects these statically in Phase A and injects targeted translation guidance into Phase B prompts. Each node only receives guidance for the idioms present in its own source \u2014 no noise, no irrelevant context.</p>"},{"location":"reference/idioms/#detected-idioms-msagl-js-counts","title":"Detected idioms (msagl-js counts)","text":"Idiom Occurrences Description <code>mutable_shared_state</code> 1,608 Objects mutated after being passed to a function <code>null_undefined</code> 1,171 <code>== null</code>, <code>?.</code>, <code>??</code>, nullable return types <code>dynamic_property_access</code> 945 <code>obj[key]</code> with a computed key <code>static_members</code> 920 <code>static</code> class fields and methods <code>number_as_index</code> 519 <code>number</code> used to index an array (should be <code>usize</code>) <code>closure_capture</code> 441 Closures that capture outer-scope variables <code>array_method_chain</code> 248 <code>.map()</code> / <code>.filter()</code> / <code>.reduce()</code> / <code>.find()</code> chains <code>map_usage</code> 242 <code>Map&lt;K,V&gt;</code> usage <code>set_usage</code> 220 <code>Set&lt;T&gt;</code> usage <code>generator_function</code> 186 <code>function*</code> and <code>yield</code> <code>class_inheritance</code> 118 <code>extends</code> relationships (see Class Hierarchies) <code>union_type</code> 34 TypeScript union types (<code>A \\| B</code>) <code>optional_chaining</code> 13 <code>?.</code> operator <code>async_await</code> 7 <code>async</code>/<code>await</code> and <code>Promise</code> patterns"},{"location":"reference/idioms/#how-idiom-guidance-is-injected","title":"How idiom guidance is injected","text":"<p><code>context.py</code> loads relevant sections from <code>idiom_dictionary.md</code> by matching section headers to the node's <code>idioms_needed</code> tags:</p> <pre><code>def _load_idiom_entries(idioms: list[str], workspace: Path) -&gt; str:\n    content = (workspace / \"idiom_dictionary.md\").read_text()\n    for idiom in idioms:\n        pattern = re.compile(rf\"^##\\s+{re.escape(idiom)}\\b.*?(?=^##\\s|\\Z)\",\n                             re.MULTILINE | re.DOTALL)\n        # ... extract matching section\n</code></pre> <p>The result appears in the prompt under <code>## Idiom Translations</code>, containing only the sections relevant to the current node.</p>"},{"location":"reference/idioms/#translation-examples","title":"Translation examples","text":""},{"location":"reference/idioms/#null_undefined-optiont","title":"<code>null_undefined</code> \u2192 <code>Option&lt;T&gt;</code>","text":"TypeScriptRust <pre><code>const node = graph.findNode(id);\nif (node == null) return null;\nreturn node.label ?? \"untitled\";\n</code></pre> <pre><code>let node = graph.borrow().find_node(id);\nif node.is_none() { return None; }\nlet node = node.unwrap();\nSome(node.borrow().label\n    .clone()\n    .unwrap_or_else(|| \"untitled\".to_string()))\n</code></pre>"},{"location":"reference/idioms/#mutable_shared_state-rcrefcellt","title":"<code>mutable_shared_state</code> \u2192 <code>Rc&lt;RefCell&lt;T&gt;&gt;</code>","text":"TypeScriptRust <pre><code>class GeomGraph {\n  boundingBox: Rectangle;\n  constructor() {\n    this.boundingBox = new Rectangle();\n  }\n}\n// Shared reference: graph is passed to multiple objects\n// and they all mutate boundingBox\n</code></pre> <pre><code>pub struct GeomGraph {\n    pub bounding_box: Rectangle,\n}\n\n// Shared, mutable reference:\nlet graph: Rc&lt;RefCell&lt;GeomGraph&gt;&gt; = Rc::new(RefCell::new(GeomGraph { ... }));\n// Read:  graph.borrow().bounding_box\n// Write: graph.borrow_mut().bounding_box = r;\n</code></pre>"},{"location":"reference/idioms/#array_method_chain-iterators","title":"<code>array_method_chain</code> \u2192 iterators","text":"TypeScriptRust <pre><code>const result = nodes\n  .filter(n =&gt; n.isVisible)\n  .map(n =&gt; n.label)\n  .find(l =&gt; l.startsWith(\"main\"));\n</code></pre> <pre><code>let result = nodes.iter()\n    .filter(|n| n.borrow().is_visible)\n    .map(|n| n.borrow().label.clone())\n    .find(|l| l.starts_with(\"main\"));\n</code></pre>"}]}